import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'space.nicecalendar.www',
  appName: 'Nice Calendar',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
